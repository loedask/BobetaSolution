namespace Bobeta.Landing;

/// <summary>Used by the PWA router to discover landing routes. When splitting marketing from the app, publish this project separately.</summary>
public static class LandingAssemblyMarker;
