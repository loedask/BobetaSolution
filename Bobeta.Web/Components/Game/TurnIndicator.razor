<div class="rounded-xl px-4 py-2 text-center text-sm font-medium @(IsPlayerTurn ? "gold-gradient text-primary-foreground" : "bg-secondary text-secondary-foreground")">
    @(IsPlayerTurn ? "Your turn" : "Opponent's turn")
</div>

@code {
    [Parameter] public bool IsPlayerTurn { get; set; }
}
