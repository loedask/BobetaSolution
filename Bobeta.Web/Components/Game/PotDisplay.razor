<div class="rounded-xl border border-border bg-card px-4 py-3 flex items-center justify-between">
    <span class="text-sm text-muted-foreground">Pot</span>
    <span class="gold-text font-display text-xl font-bold">@PotAmount.ToString("N0") FCFA</span>
</div>

@code {
    [Parameter] public decimal PotAmount { get; set; }
}
